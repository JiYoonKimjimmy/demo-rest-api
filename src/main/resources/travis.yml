language: java
jdk: openjdk8

branches:
  only: master

cache:
  directories: $HOME/.gradle'

before_install: chmod +x gradlew

script: "./gradlew clean build"

notifications:
  email:
    recipients: jimmyberg.kim@gmail.com
